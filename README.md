# netdom
netdom is a lightweight, modular subdomain enumeration script that combines passive OSINT (crt.sh, VirusTotal, ThreatCrowd) with active DNS brute-force and HTTP probing. It outputs results in JSON and plain-text formats and is fully configurable via a simple settings file.
